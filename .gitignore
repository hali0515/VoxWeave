__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
*.lock
# ...except the semantic worker's uv lock: `uv run --locked --script` requires it
# at runtime and it ships as package data (see pyproject [tool.setuptools.package-data]).
!voxweave/semantic_worker.py.lock
build/
dist/
debug/
cache/

TODO.md

# 私有项目说明, 不入库
CLAUDE.md

# private session handoff log
log.md
